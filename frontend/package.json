{
  "name": "foodbridge-enterprise",
  "version": "1.0.0",
  "description": "FoodBridge - Single Origin Enterprise Architecture",
  "scripts": {
    "build:css": "npx tailwindcss -i ./styles/main.css -o ./styles/styles.css --minify",
    "watch:css": "npx tailwindcss -i ./styles/main.css -o ./styles/styles.css --watch",
    "lint": "echo 'Frontend lint: HTML/CSS validation' && exit 0",
    "serve": "npx live-server --port=5500 --open=/pages/index.html",
    "test:smoke": "playwright test --config=playwright.visual.config.js",
    "test:smoke:headed": "playwright test --config=playwright.visual.config.js --headed",
    "test:visual": "playwright test --config=playwright.visual.config.js --pass-with-no-tests",
    "start": "npm run serve",
    "dev": "npm run serve",
    "install:all": "npm install && cd frontend && npm install"
  },
  "keywords": [
    "food-donation",
    "ngo",
    "volunteer",
    "tailwind",
    "html",
    "foodbridge",
    "express",
    "single-origin"
  ],
  "author": "FoodBridge DevOps Team",
  "license": "MIT",
  "dependencies": {
    "chart.js": "^4.4.2",
    "cors": "^2.8.5",
    "cross-env": "^7.0.3",
    "css": "^3.0.0",
    "css-selector-parser": "^3.3.0",
    "dotenv": "^16.4.5",
    "glob": "^13.0.6"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0",
    "live-server": "^1.2.0",
    "nodemon": "^3.1.0",
    "tailwindcss": "^3.4.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
